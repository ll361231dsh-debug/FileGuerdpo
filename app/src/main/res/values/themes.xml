<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.FileGuardPro" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary branding color -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorOnPrimary">@color/on_primary</item>

        <!-- Backgrounds -->
        <item name="android:windowBackground">@color/background_color</item>
        <item name="colorSurface">@color/surface_color</item>
        <item name="colorOnSurface">@color/on_surface</item>

        <!-- Use MaterialComponents attributes for a modern look -->
        <item name="materialCardViewStyle">@style/Widget.FileGuardPro.GlassCard</item>
    </style>

    <style name="Widget.FileGuardPro.GlassCard" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardUseCompatPadding">true</item>
        <item name="cardElevation">4dp</item>
        <item name="cardBackgroundColor">@color/surface_color</item>
    </style>
</resources>
